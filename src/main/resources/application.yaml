server:
  port: 8080
  servlet:
    context-path: /warehouse

spring:
  datasource:
    url: "jdbc:mysql://localhost:3308/warehouse"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true